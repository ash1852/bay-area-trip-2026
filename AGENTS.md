# 维护约定

- 这是公开的静态行程地图，不添加后端、数据库、登录或复杂框架。
- 行程、地点、预算、预约事项统一维护 `data/itinerary.js`。普通行程修改不改地图逻辑。
- `app.js` 负责地图与时间表共同渲染，不能维护第二份时间数据。
- 时间均为地点当地时间；未订班次必须标明待预约/待核实。未知费用用 null，不用 0。
- 路线几何统一存入 data/route-paths.js，与行程时间解耦；实线有路网或线路来源，未知路线与接入段显示虚线。不冒充实时导航。更新地点坐标后须更新对应几何，否则自动回退示意。
- 持续保留 10/5 晚自由时间、10/5–6 完整上午会议、10/10 OAK 18:50 航班。
- 不添加订单号、邮箱、身份证件、二维码、支付信息或仓库凭证。
- 修改后运行 `node scripts/validate.mjs`；涉及界面则检查日期切换、两种模式、移动端和重复地点节点。
- 发布前先读取远端最新版本，保留用户改动。提交 main 触发 GitHub Pages；核实部署成功后才能说已上线。
